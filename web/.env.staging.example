# Public browser-exposed staging frontend values only. Do not store secrets here.
# VITE_MAPLECARD_API_BASE_URL must be the backend origin only, with no /api suffix.

VITE_MAPLECARD_API_MODE=backend
VITE_MAPLECARD_API_BASE_URL=https://your-railway-backend-domain